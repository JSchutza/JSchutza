 You will need to authenticate with your personal service account in GCP. 
   Use the following command to do so.
    cd into the dir where this service account file is then run:
`cat main_service_portfolio.json | docker login -u _json_key --password-stdin https://us.gcr.io`
